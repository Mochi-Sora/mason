{
 "type": "function",
 "function": {
  "name": "focus_pane",
  "description": "Reveal and focus a Mason desktop pane when the user asks to see it: chat, files, terminal, review (git diff), or sessions. For URLs/files use the desktop_preview tool instead.",
  "parameters": {
   "type": "object",
   "properties": {
    "pane": {
     "type": "string",
     "enum": [
      "chat",
      "files",
      "terminal",
      "review",
      "sessions"
     ],
     "description": "Which pane to reveal."
    }
   },
   "required": [
    "pane"
   ]
  }
 }
}
