{
 "type": "function",
 "function": {
  "name": "write_state",
  "description": "Rewrite the session's tiny state.md (under 2000 chars, bullets only).",
  "parameters": {
   "type": "object",
   "properties": {
    "content": {
     "type": "string",
     "description": "Full new state.md."
    },
    "session_id": {
     "type": "string",
     "description": "Session id."
    }
   },
   "required": [
    "content"
   ]
  }
 }
}
